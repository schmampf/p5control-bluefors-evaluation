import numpy as np
import matplotlib.pyplot as plt


# list = [
#     np.array([0, 0, 0, 0, 0]),
#     np.array([0, 1, 0, 1, 0]),
#     np.array([0, 0, 0, 0, 0]),
#     np.array([0, 1, 1, 1, 0]),
#     np.array([0, 0, 0, 0, 0]),
# ]

# data = np.empty((5, 5))

# for i, val in enumerate(list):
#     data[i, :] = list[i]

# plt.imshow(data)
# plt.colorbar()
# plt.show()

idx = np.linspace(0, 1000, 11)
print(idx)
